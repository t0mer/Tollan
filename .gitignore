# Claude / AI assistant context — local only, never track
CLAUDE.md
.claude/
.cursor/
.aider*

# Design / implementation / planning docs — local only
/docs/
docs/design/
*.design.md
*.implementation.md

# Security scan artifacts
scans/

# Build output
/dist/
/bin/
/build/

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
coverage.*
*.coverprofile
profile.cov
go.work
go.work.sum
vendor/

# Frontend
web/node_modules/
web/.vite/
node_modules/
*.log
npm-debug.log*

# Runtime data / secrets
/data/
*.db
*.db-journal
*.sqlite
*.sqlite3
*.mmdb
.env
.env.*
!.env.example

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store

# Built frontend is embedded at build time, not tracked — but keep the
# placeholder so go:embed compiles before any frontend build.
web/dist/*
!web/dist/.gitkeep
